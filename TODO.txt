Create constant tasks (e.g. updating progress bar with duration)
Adjust Sizing of items to ensure everything fits in the window
Create Playlists
Tap into file's metadata to display title, album cover, duration, album

